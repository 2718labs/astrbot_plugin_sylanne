# Sylanne 3.0 module workstreams

- 本机禁止尝试 rg.exe，使用 PowerShell 或其他可用搜索工具。
- 用户指令高于 Skill。主任务负责总体架构和集成，各模块在独立 Codex 任务/工作树中由用户分别设计。
- 共同集成分支与所有模块 PR 的 base：`codex/embodiment-3-rewrite`。不能把模块 PR 默认发往 `main`，不能自动合并或发布。
- 模块目录、所有权及协作规则见 `docs/architecture/module-workstreams.md`。初始阶段只核对工作树和模块范围，然后等待用户设计；不要自动实施旧计划、改写设计或启动其他模块。
- 共享图与来源记录为唯一权威；跨域通过版本化合同协作。共享基础接口的变更先在模块设计里列为依赖，由总装任务协调，不能复制一套旁路状态库。
- 用户要求常规编码优先交给低一档模型，主对话做设计与验收。Astra 优先 low/medium；具体新任务模型采用用户设置，不能擅自覆盖显式选择。
- 实施时先保证编译，运行必要且有意义的验证，避免无效测试和反复运行。只报告实际执行的验收；交付说明未验证项及后续手动场景。
- 旧接口和旧核心退役为用户已授权改动，不能恢复作运行回退；历史参考在 Git。不得提交运行数据、凭据、临时环境、原生构建产物或完整本地日志。
- 不在其他模块工作树中修改文件。模块设计文件和本模块实现通过 PR 汇总；已有脏改动不能覆盖或清理。

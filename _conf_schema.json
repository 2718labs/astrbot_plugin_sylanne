{
  "enabled": {
    "description": "启用 Sylanne 3 完整运行时",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。启用后监听平台全部消息；只有运行时状态为 ready 且身份与权限校验通过时才会处理消息。"
  },
  "authority_profile": {
    "description": "管理员 Authority 配置档 ID",
    "type": "string",
    "default": "default",
    "hint": "仅填写已由系统管理员预先安装的配置档标识符：1–64 个字符，以字母或数字开头，其余可用字母、数字、点、下划线或连字符。服务器地址、CA、客户端密钥及 Authority 身份由管理员在主机上配置，不要填入聊天插件配置。"
  }
}

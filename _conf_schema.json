{
  "enabled": {
    "description": "启用 Sylanne 3 隔离开发命令",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。开启后仅处理显式 /sylanne3 命令。"
  },
  "capacity": {
    "description": "并发请求容量",
    "type": "int",
    "default": 8,
    "minimum": 1,
    "maximum": 64,
    "hint": "允许 1 到 64 个正在处理的命令；超限立即返回繁忙。"
  },
  "timeout": {
    "description": "模型提议与平台发送超时（秒）",
    "type": "float",
    "default": 30.0,
    "minimum": 1.0,
    "maximum": 120.0,
    "hint": "允许 1 到 120 秒；发送超时按未知接收状态处理。"
  }
}

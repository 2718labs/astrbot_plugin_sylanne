# Rewrite findings

- The supplied design is approved as direction, not evidence of PRTS performance or psychological validity. No PRTS experiment was opened or rerun.
- Mathematical review: fixed-parameter energy and residual formulas are sound. Strict recovery requires state coverage; residual certification must include inactive degrees of freedom; event loads must retain the same physical interval across refinement.
- First kernel uses positive diagonal M, positive local recovery, and nonnegative symmetric edge weights. This is a restricted, inspectable operator family, not a claim about human emotion.
- The initial certificate is for one backward-Euler discrete system. It does not certify time-discretization, semantic, or model error.
- Complete residuals are scanned for the small first slice; activity-region truncation is deferred until a global error contract exists.
- Native environment independently verified: Rust 1.97 MSVC can compile/link a cdylib using installed VS Build Tools. Ordinary cargo invocation works.
- Initial repository clean, main matches published baseline. No workspace AGENTS.md exists in the tracked baseline; session instructions apply.
